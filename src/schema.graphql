#import { Module } into Subgraph from "ens/graph-node.polywrap.eth"

type Module {
  getAccountData(
    subgraphAuthor: String!
    subgraphName: String!
    account: String!
  ): JSON!
  getAccountGoals(
    subgraphAuthor: String!
    subgraphName: String!
    account: String!
  ): JSON!
}
